# Cloudflare Workers 环境变量配置
# 在 Cloudflare Dashboard 中配置这些环境变量
# 路径: Workers & Pages > v0tv > Settings > Variables

# =============================================================================
# 必填配置
# =============================================================================

# 访问密码（必填）
PASSWORD=your_password

# =============================================================================
# 多用户配置（可选 - 使用 D1 数据库时）
# =============================================================================

# 存储类型
# - localstorage: 纯客户端存储（默认，无需数据库）
# - d1: Cloudflare D1 数据库（支持多用户，数据持久化）
NEXT_PUBLIC_STORAGE_TYPE=d1

# 管理员用户名（使用 D1 时需要）
USERNAME=admin

# 是否允许用户注册
NEXT_PUBLIC_ENABLE_REGISTER=true

# =============================================================================
# D1 数据库配置说明
# =============================================================================
#
# 如果使用 D1 数据库，需要：
#
# 1. 创建数据库：
#    npx wrangler d1 create v0tv-db
#
# 2. 在 wrangler.jsonc 中配置 D1 binding（取消注释并填入 database_id）
#
# 3. 数据库表会在 Worker 首次运行时自动创建（无需手动运行 SQL 脚本）
#
# 4. 部署：
#    pnpm run pages:build && npx wrangler deploy
#
# 详细配置指南请查看: CLOUDFLARE_D1_AUTO_SETUP.md
#

# =============================================================================
# 其他可选配置
# =============================================================================

# 站点名称
SITE_NAME=V0TV

# 站点公告
ANNOUNCEMENT=欢迎使用V0TV影视聚合播放器

# 搜索最大页数（1-50）
NEXT_PUBLIC_SEARCH_MAX_PAGE=5

# 图片代理（解决跨域问题）
NEXT_PUBLIC_IMAGE_PROXY=

# 豆瓣数据代理
NEXT_PUBLIC_DOUBAN_PROXY=
