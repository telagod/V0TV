# Cloudflare Workers 环境变量配置
# 在 Cloudflare Dashboard 中配置这些环境变量
# 路径: Workers & Pages > v0tv > Settings > Variables

# =============================================================================
# 必填配置 - D1 多用户模式（推荐）
# =============================================================================

# 访问密码（必填）
PASSWORD=your_password

# 存储类型 - 默认使用 D1 数据库
# - d1: Cloudflare D1 数据库（推荐，支持多用户和管理面板）
# - localstorage: 纯客户端存储（单用户模式）
NEXT_PUBLIC_STORAGE_TYPE=d1

# 管理员用户名（D1 模式必填）
USERNAME=admin

# 是否允许用户注册
NEXT_PUBLIC_ENABLE_REGISTER=true

# =============================================================================
# D1 数据库配置说明
# =============================================================================
#
# 🎉 好消息！D1 数据库会在首次部署时自动创建！
#
# Wrangler 4.45.0+ 支持自动 provisioning：
# ✅ 无需手动创建数据库
# ✅ 无需运行 SQL 脚本
# ✅ 数据库表会在 Worker 首次运行时自动初始化
#
# 只需确保：
# 1. wrangler.jsonc 中已配置 d1_databases binding（已配置）
# 2. 在 Dashboard 设置上述环境变量
# 3. 运行部署命令：pnpm run pages:build && npx wrangler deploy
#
# 首次部署后，wrangler.jsonc 会自动更新填入 database_id
#
# 详细说明请查看: CLOUDFLARE_D1_AUTO_SETUP.md
#

# =============================================================================
# 其他可选配置
# =============================================================================

# 站点名称
SITE_NAME=V0TV

# 站点公告
ANNOUNCEMENT=欢迎使用V0TV影视聚合播放器

# 搜索最大页数（1-50）
NEXT_PUBLIC_SEARCH_MAX_PAGE=5

# 图片代理（解决跨域问题）
NEXT_PUBLIC_IMAGE_PROXY=

# 豆瓣数据代理
NEXT_PUBLIC_DOUBAN_PROXY=
